const express = require("express") /*Serve para importar o express */
const path = require("path") /*Lida com leitura de caminhos dos arquivos */

let initialPath = path.join(__dirname, "public") /*Ele vai pegar a pasta inicial e j  */